package app;

public class MyCircle {
	
	private center = MyPoint;
	private radius int = 1;
	
	public MyCircle(int x, int y, int radius) {
		this.x = x
		this.y = y
		this.radius = radius;
	}
	
	public MyCircle(center MyPoint, int radius) {
		this.MyPoint = MyPoint;
		this.radius = radius;
	}